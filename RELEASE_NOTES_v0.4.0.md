# Evolabs 0.4.0 — 逐鏡重做與安全本機對嘴

## 新增

- 動漫與寫實模式都可使用單角色 IP-Adapter 參考圖。
- 參考圖改為內容雜湊資產，專案檔只保存路徑；舊 Base64 參考圖在載入時自動遷移。
- 真正逐鏡預覽保存與顯示；失敗或取消後，已完成鏡頭的預覽仍可檢查。
- 指定單鏡生成／重做：保留原始鏡號，AI 重做會換 Seed，完整版可重用內容快取。
- 可取消、可逾時的模型子程序與跨程序 GPU 鎖。
- MuseTalk 1.5 本機自備 Provider 與渲染接線：只在 CUDA、顯存、完整依賴及 CLI 探測均通過時啟用；單人、25 FPS、最長 30 秒。
- 故事快速規劃改為從使用者內容擷取角色與對白，不再產生固定示範人物。
- Windows 來源碼建置器固定 Node.js 24，改善 Rust／MSVC／WebView2／重新開機提示。

## 安全與資料完整性

- 匯入參考圖會驗證 MIME 與真實 PNG／JPG／WebP magic bytes，並限制 10 MB。
- UI 讀取本機圖片只允許 Evolabs 參考圖與工作預覽目錄，且限制大小與格式。
- 多角色參考圖不再誤稱「採一位主參考角色」；目前會停用參考條件，避免錯綁身分。
- 預覽最多保留最近 12 個終態工作、合計 512 MiB；近期執行中的工作不會被清理。

## 驗證

- 前端型別檢查、22 項測試及 Production web build。
- 67 項以上 Engine 測試，包含真實 MP4、單鏡工作、取消、GPU 鎖、預覽留存、MuseTalk fake-runtime 與渲染接線。
- 模型 manifest 雙重驗證與 Windows MSVC Rust target `cargo check`。

## 已知限制

- 此交付環境不是 Windows，無法在此產出並實機安裝最終 NSIS `.exe`。
- MuseTalk 不隨 App 散布，也沒有一鍵模型包；上游完整依賴的授權與固定分發尚未閉合。
- RTX 3050 4GB 的穩定成片路徑仍是 AI 單張畫面、2.5D 鏡頭運動、配音與合成；大型逐幀 I2V 未啟用。
- 多人精準身分鎖定、多人對嘴與專用多角色神經語音尚未完成。
