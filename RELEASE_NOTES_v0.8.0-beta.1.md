# Evolabs Agent Studio v0.8.0-beta.1

## 重要變更

### 真實 AI 對話

- 對話區只顯示使用者訊息與模型實際回覆。
- 系統活動、等待、錯誤、重試、Token 與驗證紀錄改到獨立區域。
- 移除預寫 Agent 對話及規則式交付物冒充 AI 完成的路徑。
- Runtime 離線、模型不存在、回覆無效或資料不足時直接阻止工作。
- 支援與單一 Agent 交流及多 Agent 製作會議。
- Agent 建議會形成可預覽、可套用或拒絕的專案修改提案。

### Agent 任務理解與共享記憶

- 每位 Agent 必須先回報任務目標、收到的資料、限制與缺少資訊。
- 缺少資料時工作標記為受阻，不得產生正式成果。
- 所有 Agent 使用最新劇本、角色、場景、分鏡、使用者要求及影片工作流能力。
- 導演可將不合格成果退回指定 Agent，並限制最大修正輪次。
- 保存模型、request ID、延遲、Token、驗證與交付物證據；不顯示隱藏思考過程。

### 真正影片模型管線

- AI 影片模式改為逐鏡呼叫本機 ComfyUI API 影片工作流。
- 工作流必須包含提示詞、負向提示、Seed、幀數、幀率與逐鏡輸出名稱綁定。
- 驗證 ComfyUI 節點、工作流結構與影片輸出能力。
- 只輸出 PNG／JPG 的工作流會被拒絕，不能冒充 AI 影片。
- 參考圖轉影片工作流會在生成前確認每鏡頭只有一名角色，且已匯入可追溯的角色身份參考圖。
- 每個鏡頭執行解碼、時長、黑畫面與凍結畫面檢查。
- 每個鏡頭必須經使用者人工核准；退回後可附註並限次重新生成。
- 舊版圖片加 FFmpeg 運鏡改為獨立「動態漫畫」模式，明確標示不是影片模型生成。

### 人物與內容品質

- 固定角色年齡、身份、完整服裝與外觀規則。
- 新增角色身份參考圖匯入、預覽、更換與移除；變更後相關鏡頭及舊成片會立即失效。
- 增加禁止裸露、額外眼睛／臉部／頭部／肢體、年齡漂移與服裝漂移等約束。
- 逐鏡審核加入人物、服裝、年齡、動作及連戲檢查清單。
- 本 Beta 不宣稱已內建可可靠辨識所有人體畸形或裸露問題的視覺分類模型。

### 模型、設定與介面

- Agent 模型與影片模型服務分開管理與驗證。
- 支援匯入不同本機 ComfyUI API 工作流，以切換真正影片模型。
- 重製模型、本機執行環境、設定、製作、鏡頭審核與診斷頁面。
- 修正文字逐字直排、內容溢出、欄位錯位與過小字級。
- 統一正式繁體中文名詞與狀態文案。
- 舊版 SD 1.5 模型重新標示為「動態漫畫／參考圖模型（選配）」。

## Beta 限制

- RTX 3050 Laptop 4 GB 對真正影片模型僅屬實驗性相容。
- 實際速度與可用性取決於 ComfyUI 工作流、模型、量化、offload、解析度與幀數。
- 真正影片模式目前不啟用未經驗證的本機口型同步。
- 升為穩定版前仍需完成 Windows 實機多鏡頭端到端生成驗證。
