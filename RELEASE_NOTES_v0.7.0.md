# Evolabs v0.7.0

## 產品介面重製

- 重新設計為克制、單色、可靠的桌面工作室，不使用多餘動畫或花俏裝飾。
- 把舊版難以理解的無限畫布首頁改成清楚的 **劇本 → 編劇交付 → 完整製作 → 生成成片** 流程。
- 新增獨立的製作、模型與設定頁面，所有 Runtime、模型、儲存與生成狀態都有明確文字回饋。
- 重新製作 Evolabs 幾何字標與 Windows 應用程式圖示。

## 編劇與交互可靠性

- 使用者送出劇本後，先顯示編劇已接收的文字與實際交付內容，再讓使用者進入後續製作。
- 本機模型輸出無法解析時，會明確標示由內建專家接手，不再假裝所選模型成功。
- 選擇不存在或尚未載入的模型時會阻止送出，並直接引導到模型管理頁。
- 壓縮過長的劇本與共享製作資料，降低 8K context 本機模型因輸入超限而失敗的機率。
- 調整各 Agent 的輸出預算，避免小型本機模型因輸出 token 過大而中斷。

## 多模型與 Runtime

- 新增 Agent Model Gateway，從本機 `/v1/models` 讀取實際已載入模型。
- 支援自動選擇、選擇任一已載入模型，以及手動指定本機模型 ID。
- 所有自訂模型 ID 都由 Rust 後端重新驗證，且 Agent 端點仍限制為 loopback。
- 視覺模型包與文字 Agent 模型分開管理，安裝、授權、修復與健康狀態集中呈現。

## 發布與品質

- GitHub Actions 新增 Rust 命令測試、版本同步與正式圖示生成。
- 保留 Tauri updater 簽章、NSIS 安裝器、`.sig` 與 `latest.json` 發布鏈。
- 已安裝 Evolabs 的使用者可從應用程式內檢查更新並重新啟動。
