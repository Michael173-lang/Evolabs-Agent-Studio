# Evolabs Agent Studio v0.8.0-beta.2

## 本次更新重點

### AI 製片團隊協作

- 製作會議改為依專業順序執行，總導演於其他成員完成交付後進行最後統整。
- 每位成員都會收到團隊名單、既有交付、前序模型回覆及專案限制，不再各自重複詢問相同問題。
- 成員可向其他專業成員提出結構化協作要求；協作結果會回到原成員進行整合。
- 一般創作細節由負責成員依劇本安全補齊，只有必須由使用者決定的核心矛盾才會中止工作。
- 受阻或失敗階段可以續跑，不必重新執行已完成的整套流程。

### 對話可靠性與操作體驗

- 送出訊息後立即清空輸入框，並建立可追蹤的使用者訊息狀態。
- 顯示目前回覆成員、執行階段、成員進度及經過時間。
- 製作會議可在目前成員回覆完成後停止，既有成果會保留。
- 未完成或部分完成的訊息可直接重試，不必重新複製內容。
- 新增總導演統整、團隊補齊、列出阻塞及建立修改提案等常用指示。
- 對話區只顯示使用者訊息與實際模型回覆；模型呼叫、驗證、重試及錯誤維持獨立記錄。

### HTTP 400 與模型相容性

- 對 OpenAI 相容服務加入多階段請求格式重試。
- 模型不支援 `response_format`、`max_tokens` 或要求較短 Context 時，會自動切換相容策略。
- 自動限制單則訊息、歷史訊息數量及總內容長度，降低 Context 過大造成的 HTTP 400。
- 支援多種常見回覆欄位名稱；模型仍未提供有效最終回覆時，保留原訊息並提供重試。
- 模型執行紀錄會顯示重試次數與原因。

### 受管理的 AI 影片引擎

- Evolabs 可在應用程式內安裝、啟動、停止、修復及解除安裝受管理的 ComfyUI 執行環境。
- 安裝前檢查系統工具及可用磁碟空間，並為低顯存裝置套用背景啟動參數。
- 已安裝的引擎可於 Evolabs 啟動時自動啟動。
- 可選擇只解除安裝執行環境並保留影片模型，或連同模型一併移除。
- 自訂 ComfyUI API 工作流保留於進階設定；Evolabs 仍會驗證節點、參數綁定及影片輸出能力。

### 儲存空間與模型管理

- 顯示所在磁碟的總容量、已使用空間、可用空間及 Evolabs 總占用。
- 分類顯示影片模型、舊模型版本、下載快取、生成快取、生成輸出、工作暫存與角色參考素材。
- 支援個別解除安裝模型、刪除輸出與快取，以及開啟實際檔案位置。
- 可一次清除所有未啟用的舊模型版本；目前啟用的版本不會被批次清除。
- 刪除前會顯示預計釋放空間與不可復原警告。
- 儲存管理只接受 Evolabs 掃描後產生的安全識別碼，不接受任意路徑。

### 介面與文案

- 模型、設定、對話、錯誤與儲存管理文案統一為正式繁體中文。
- 移除模糊或開發者口吻，改為清楚說明目前狀態、影響與下一步。
- 儲存管理、影片引擎進度、模型解除安裝及團隊協作使用一致的狀態樣式。

## 建置與發布

本版本改為由使用者在本機控制建置與發布：

1. 執行 `1_BUILD_AND_TEST.bat`，完成 Windows 測試並產生安裝程式與 updater 簽章。
2. 安裝並實際測試後，執行 `2_PUBLISH_RELEASE.bat`，上傳已建置完成的發行檔案；發布階段不會重新編譯。
